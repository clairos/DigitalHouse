const dividir = require('./dividir');
const multiplicar = require('./multiplicar');
const somar = require('./somar');
const subtrair = require('./subtrair');

const resDiv = dividir(0,7);
const resSom = somar(1,6);
const resMul = multiplicar(0,7);
const resSub = subtrair(4,2);

console.log(resDiv);
console.log(resSom);
console.log(resMul);
console.log(resSub);